/**
 * Values that never change through the life of the app.
 */

define([],
function() {
    
    var Constants = {
        // dimensions
        MAX_SCREEN_WIDTH: 1024,

        // fonts
        

        // colors 
        

        // urls
        

        // keys 
        LOCAL_PREFS_STORAGE_KEY: 'nameOfApplication'
    };

    return Constants;
});